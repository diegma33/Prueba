<UserControl
    x:Class="Prueba.UserControls.TeamImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Prueba.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Button Click="Button_Click" Margin="7,24,0,258"></Button>
        <GridView x:Name="ViewControl" ItemsSource="{Binding Teams}" HorizontalAlignment="Left" VerticalAlignment="Top">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="25">
                        <Image x:Name="image" Margin="0,0,0,0" Source="{Binding imgPath}" Height="207" Width="208" Stretch="Fill"/>
                        <TextBlock x:Name="textBox" Text="{Binding name}" Foreground="White" FontSize="25" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
